public class notes22 {

    public static void main(String[] args) {

        System.out.println("2.2 notlari");
    }
    
}


/*
 * When creating an object from a class, it can be called with certain parameters or with no parameters
 * depending on how the constructor is formed. These parameteres overload the constructor to modify
 * certain attributes or methods. 
 * 
 * An object is created like this
 * 
 * className objectName = new className(); PARAMETERS IF NEEDED
 * 
 * You can set an object to be null but it makes no sense as it does more harm than good in general
 * 
 * A constructor signature (e.g. Public) determines the availabilty of the class (scope) and
 * a class header determines standard values for parameters that the constructor can accept (if any
 * parameters needed)
 * 
 * Actual parameters are the arguments passed when creating an object, formal parameters
 * are the parameters defined in the class constructor.
 * 
 * 
 */